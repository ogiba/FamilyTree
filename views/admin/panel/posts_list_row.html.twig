{% block postListRow %}
<tr onclick="window.location+='/post?id={{ post.id }}';">
    <th scope="row">{{ loop.index }}</th>
    <td>{{ post.title }}</td>
    <td>{{ post.shortDescription }}</td>
    <td>{{ post.author }}</td>
    <td>{{ post.timeStamp }}</td>
</tr>
{% endblock %}